album_api_version: 0.5.1
args:
- description: Path to the input Zarr zip store containing the features and labels.
  name: input_zarr_path
  required: true
  type: string
- description: Path for the output joblib file containing the trained Random Forest
    model.
  name: output_model_path
  required: true
  type: string
- description: Number of trees in the Random Forest.
  name: n_estimators
  required: true
  type: string
- description: Minimum feature size to include in the training data.
  name: min_feature_size
  required: true
  type: string
changelog: Handle ragged feature sets by dropping any undersized arrays
description: A solution that trains a Random Forest model using data from a Zarr zip
  store, filters runs with only one label, and performs 10-fold cross-validation.
group: cellcanvas
license: MIT
name: train-model
solution_creators:
- Kyle Harrington
tags:
- random forest
- machine learning
- segmentation
- training
- cross-validation
timestamp: '2024-05-21T09:28:10.489234'
title: Train Random Forest on Zarr Data with Cross-Validation
version: 0.1.1
